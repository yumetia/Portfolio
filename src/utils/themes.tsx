const themes = [
    "dracula",
    "emerald",
    "cyberpunk",
    "aqua",
].sort((a, b) => a.localeCompare(b));
// order matters in the button rendering, so sorting the array

export default themes
